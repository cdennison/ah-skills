# Unfinished Tasks

- Test whether keyword search, rerank, etc. are actually working (verify the search pipeline stages produce correct/expected results, not just that they run).
- Add stats on what's already in the DB vs. net new (e.g. per pipeline run: count of skills already indexed vs. newly discovered/added), so ingestion runs are auditable.
- Zip file upload/download and recreating this repo fresh on a new computer (from `search-raw.zip` / `make_data_zip.sh`) hasn't been tested end-to-end.
- Resume cloning the 76 repos approved from the 2026-08-01 `"agents skills"` search (`repo-seeds/github_search_results.json`, top 100) once local disk space is freed up. `run_pipeline.sh` died mid-`clone_repos.py` with `ENOSPC` after cloning 60 of the 76; those 60 partial clones were deleted from `repos/` and their entries stripped from `.clone_state.json` so they're not skipped as "recently cloned." All 76 are already approved in `repo-seeds/registry.json` (source=search) — nothing needs re-approving. To resume:
  1. Free up local disk space (system was at 99% used, ~2.9Gi free) — check with `df -h .`.
  2. Run `./run_pipeline.sh` (or just `python3 clone_repos.py` first if you want to sanity-check clones before extract/index run).
  3. Watch for `ENOSPC` again partway through; if it recurs, re-run the cleanup: find `repos/<owner>/<repo>` dirs with a recent mtime, `rm -rf` them, and strip matching keys from `.clone_state.json` before retrying.
- Stop treating `registry.json` as an unconditional source of truth for what should be on disk — need a way to delete a cloned repo under `repos/` and have it *stay* deleted (not get re-cloned by the next `run_pipeline.sh`/`clone_repos.py` run) until explicitly told to resync.
- Need a configurable time delta for re-cloning (currently hardcoded 24h skip via `.clone_state.json` in `clone_repos.py`) — should be adjustable, not fixed.
- `make_data_zip.sh` should exclude `repos/` contents except seed-sourced repos — non-seed repos (search/manual/marketplace) shouldn't be bundled into `search_demo_data.zip`, only seed repos should be kept in the zip.
- Dashboard columns need to be synced with metadata (fields shown don't match what's actually tracked/available).
- Add a "last updated" column (date of the last git change to each SKILL.md) to the Qdrant payload, search results, Streamlit UI, and `export_csv.py` — currently no freshness signal exists anywhere in the pipeline. Blocked on a decision about how to source the date, since `clone_repos.py` does `git clone --depth 1` today:
  1. **Full clone** — drop `--depth 1` in `clone_repos.py`, then read it locally via `git log -1 --format=%cI -- <path>`. Accurate, no extra API calls, but slower/heavier clones across ~300 repos.
  2. **GitHub API per-file lookup** — call the commits API (`?path=...`) per SKILL.md. Keeps clones shallow, but ~10k calls (one per unique file) against the 5000/hr authenticated rate limit — a multi-hour backfill.
  3. **Tip-commit date as approximation** — keep `--depth 1`, record the shallow clone's single commit date. Free and immediate, but wrong whenever the file wasn't touched by the latest commit — only good enough for "repo freshness," not per-skill accuracy.
  Decision not yet made — pick an approach before implementing.
- Resume cloning the `skills.sh`-sourced repos (1103 unique GitHub repos added to `registry.json` by `add_skillsh_leaderboard.py` from the top-9558 leaderboard pull). Stopped by explicit user request (not a crash) partway through a `--source skills.sh 1000` run — 550 of the 1103 are cloned so far. No cleanup needed: the run was killed with `SIGTERM` between clones, not mid-clone, so `repos/` and `.clone_state.json` are consistent (nothing partial to strip, unlike the `ENOSPC` incident above). Note this run *did* hit `ENOSPC` once earlier and was resumed cleanly after freeing disk space — `clone_repos.py` skipping already-on-disk repos made that resume trivial, so the same approach applies here. To resume:
  1. Check disk space first — `df -h .` (was ~6.1Gi free when stopped; watch it, this batch alone used several GB for 550 repos and there are ~550 left).
  2. Run `python3 clone_repos.py --source skills.sh 1000` (or a smaller batch, e.g. `--source skills.sh --debug` first to sanity-check). Already-cloned repos are skipped automatically.
  3. If it dies with `ENOSPC` again, free space and just re-run the same command — no state cleanup required this time, since `clone_repos.py` only marks a repo as cloned in `.clone_state.json` *after* a successful clone.
  4. Once cloning is caught up, run `extract_search_raw.py` + `index_qdrant.py` (or `./run_pipeline.sh`) to get the new repos into Qdrant.
- ~~Surface leaderboard/search ranking as a column in the pipeline output~~ DONE:
  - `registry.add_search_results()` now records each result's position as `rank` on the `"search"` source descriptor (registry.py).
  - `registry.py update-skillsh owner/repo --rank N --skill-count N --top-installs N` added: partial-update CLI for a single repo's `skills.sh` descriptor, independent of clone/index state (only touches registry.json).
  - `index_qdrant.py` builds a generic `ranking` payload field (`_ranking_string()`): namespaced `{source_type}_{field}=value` tokens (e.g. `skills_sh_rank=3193 skills_sh_top_installs=3019 search_rank=12`) over ALL numeric fields on ALL of a repo's source descriptors — an N-sources x N-stats matrix, not hardcoded per source type, so a future source with its own numeric fields shows up automatically.
  - `index_qdrant.py --metadata-only` added (`refresh_metadata()`): re-derives `stars`/`sources`/`ranking` for every existing point straight from `registry.json` and does a payload-only `set_payload` push — no re-embedding, no dependency on `search-raw/`/`repos/` having the repo's files on disk. This is the "update rank for a repo that's since been deleted from disk" path.
  - `export_csv.py` now includes `"ranking"` in `FIELDS`.
  - Not done: aligning `top-40.csv`'s separate `install count` column to the same convention (that file is hand-built, not generated by `export_csv.py`).
