# ARCHITECTURE_PUBLISHING_SCANS — moved

This document moved to `vettd-e2e` on 2026-09-05: it's a cross-system
contract other repos (`vettd`, `vettd-cli`) must agree with, not just how
this repo operates itself. Placement rule:
`vettd-e2e/docs/conventions/doc-placement.md`.

**New location (sibling checkout):** `../../vettd-e2e/docs/specs/architecture-publishing-scans.md`

This repo still owns: `docs/DAILY_JOB.md`, `docs/CLI_OVERNIGHT_1ST.md`, `publish_scans.py`.
