# QUERY_SERVICE_API — moved

This document moved to `vettd-e2e` on 2026-09-05: it's a cross-system
contract other repos (`vettd`, `vettd-cli`) must agree with, not just how
this repo operates itself. Placement rule:
`vettd-e2e/docs/conventions/doc-placement.md`.

**New location (sibling checkout):** `../../vettd-e2e/docs/specs/query-service-api.md`

This repo still owns the source: `app/openapi.json`, regenerated by `scripts/render_openapi_md.sh` (which now writes the rendered copy directly into the vettd-e2e path above).
